import { hot } from 'react-hot-loader/root';

import React from 'react';

import Loading from '../Loading';

const buttonName = 'card';

const Cpp = () => <Loading buttonName={buttonName} />;

export default hot(Cpp);
